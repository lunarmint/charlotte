on:
  push:
    tags:
      - '[0-9]*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-release:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v10.0.1
        with:
          enable-cache: true

      - name: Set up Python
        run: uv python install

      - name: Install dependencies
        run: uv sync --no-dev

      - name: Download custom ffmpeg
        run: gh release download tools --pattern ffmpeg.exe --repo ${{ github.repository }}
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Build with PyInstaller
        run: uv run pyinstaller charlotte.spec

      - name: Smoke test
        run: dist/charlotte.exe --help

      - name: Upload Artifact
        uses: actions/upload-artifact@v7.0.1
        with:
          name: charlotte
          path: dist/charlotte.exe

      - name: Release
        uses: softprops/action-gh-release@v3.0.0
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: dist/charlotte.exe
          generate_release_notes: true
          draft: true
          prerelease: false